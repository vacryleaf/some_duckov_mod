# Unity 3D模型制作指南

## 📋 概述

本文档介绍如何在Unity中使用提供的编辑器工具生成名刀月影的3D模型和特效。

---

## 🛠️ 准备工作

### 1. Unity版本要求
- 推荐版本: Unity 2020.3 LTS 或更高
- 需要模块:
  - Standard Assets
  - Particle System
  - Post Processing (可选)

### 2. 导入编辑器脚本

将以下脚本复制到Unity项目的 `Assets/Editor` 目录:
- `MoonlightSwordModelGenerator.cs` - 刀模型生成器
- `SwordAuraEffectGenerator.cs` - 剑气特效生成器

脚本导入后会自动编译,并在菜单栏出现新菜单项。

---

## 🎨 生成刀模型

### 步骤1: 打开模型生成器

在Unity菜单栏选择: **Tools → 名刀月影 → 模型生成器**

### 步骤2: 调整模型参数

#### 模型尺寸
```
刀身长度: 1.2米 (可调: 0.5-2.0米)
刀身宽度: 0.08米 (可调: 0.03-0.15米)
刀身厚度: 0.02米 (可调: 0.01-0.05米)
刀柄长度: 0.2米 (可调: 0.1-0.4米)
刀柄半径: 0.025米 (可调: 0.015-0.04米)
护手宽度: 0.15米 (可调: 0.08-0.25米)
护手厚度: 0.02米 (可调: 0.01-0.04米)
```

#### 材质颜色
```
刀身颜色: RGB(232, 232, 240) - 银白色
刀身发光颜色: RGB(160, 200, 232) - 淡蓝色
护手颜色: RGB(26, 43, 74) - 深蓝色
刀柄颜色: RGB(58, 47, 90) - 深紫色
```

#### 材质属性
```
刀身金属度: 0.95 (高金属感)
刀身光滑度: 0.9 (高反射)
发光强度: 0.5 (适中发光)
```

### 步骤3: 生成模型

点击 **"生成刀模型"** 按钮,工具会自动创建:

```
MoonlightSword (根对象)
├── Blade (刀身)
│   └── BladeTip (刀尖)
├── Guard (护手)
│   ├── MoonDecoration_Left (左月牙装饰)
│   └── MoonDecoration_Right (右月牙装饰)
├── Handle (刀柄)
│   ├── Wrapping_0 (缠绕装饰1)
│   ├── Wrapping_1 (缠绕装饰2)
│   └── Wrapping_3 (缠绕装饰3)
├── Collider_Attack (攻击碰撞体)
└── Effects (特效节点)
    ├── SlashEffect (挥击轨迹)
    └── AuraEffect (剑气光环)
```

### 步骤4: 检查和调整

1. **选中模型**: 点击 "选中模型" 按钮,查看场景视图
2. **调整位置**: 确保刀模型的轴心在刀柄底部
3. **检查材质**: 确认材质正确应用,发光效果正常
4. **测试碰撞体**: 在Scene视图中查看碰撞体覆盖范围

### 步骤5: 保存Prefab

点击 **"保存为Prefab"** 按钮:
- 选择保存位置(建议: `Assets/MoonlightSword/Prefabs/`)
- 材质会自动保存到同级的 `Materials` 文件夹
- Prefab文件名: `MoonlightSword.prefab`

---

## ✨ 生成剑气特效

### 步骤1: 打开特效生成器

在Unity菜单栏选择: **Tools → 名刀月影 → 剑气特效生成器**

### 步骤2: 调整特效参数

#### 剑气尺寸
```
宽度: 2米 (横向跨度)
高度: 1米 (纵向高度)
```

#### 剑气外观
```
剑气颜色: RGB(112, 176, 224) - 冰蓝色
发光强度: 2.0 (强烈发光)
透明度: 0.7 (半透明)
```

#### 粒子效果
```
粒子数量: 100 (星点数量)
粒子大小: 0.1米
粒子生命周期: 0.5秒
粒子速度: 2米/秒
```

### 步骤3: 生成特效

点击 **"生成剑气特效"** 按钮,工具会创建:

```
SwordAuraPrefab (根对象)
├── CrescentShape (月牙形状)
├── Particles (粒子系统)
└── GlowEffect (光晕效果)
```

### 步骤4: 预览效果

1. 点击 **"预览效果"** 按钮查看粒子动画
2. 调整参数后重新生成直到满意
3. 在Scene视图中可以看到:
   - 月牙形的发光形状
   - 周围飘散的银色星点
   - 蓝色光晕

### 步骤5: 保存特效Prefab

点击 **"保存为Prefab"** 按钮:
- 选择保存位置(建议: `Assets/MoonlightSword/Prefabs/`)
- Prefab文件名: `SwordAuraPrefab.prefab`

---

## 🔧 手动调整和优化

### 优化刀模型

#### 1. 添加更多细节
```
选中Blade对象 → 在Inspector中调整:
- 添加法线贴图增强细节
- 调整UV坐标添加纹理
- 添加细分表面增加多边形
```

#### 2. 优化材质
```
选中材质 → 在Inspector中:
- 调整Smoothness创建不均匀反射
- 添加Normal Map模拟锻造纹理
- 使用Height Map创建刀刃效果
```

#### 3. 改进特效
```
选中SlashEffect → TrailRenderer组件:
- 调整Gradient创建渐变轨迹
- 增加宽度曲线变化
- 添加纹理增强视觉效果
```

### 优化剑气特效

#### 1. 粒子系统优化
```
选中Particles → ParticleSystem组件:
- Shape模块: 调整发射形状更贴合月牙
- Color over Lifetime: 创建更丰富的颜色变化
- Size over Lifetime: 调整粒子大小曲线
- Rotation over Lifetime: 添加旋转增加动感
```

#### 2. 添加额外效果
```
添加子粒子系统:
- 拖尾粒子: 跟随主体的拖尾效果
- 爆发粒子: 击中时的爆炸效果
- 消散粒子: 消失时的星点效果
```

#### 3. 光照效果
```
调整GlowEffect → Light组件:
- Intensity: 根据场景亮度调整
- Range: 影响范围
- 添加Cookie纹理创建图案投影
```

---

## 📦 导出AssetBundle

### 步骤1: 标记Asset

```csharp
// 选中MoonlightSword.prefab
// 在Inspector底部:
AssetBundle: moonlight_sword
```

对以下资源设置相同的AssetBundle名:
- `MoonlightSword.prefab`
- `SwordAuraPrefab.prefab`
- 所有材质文件
- 图标图片

### 步骤2: 创建构建脚本

在 `Assets/Editor` 创建 `BuildAssetBundles.cs`:

```csharp
using UnityEditor;
using System.IO;

public class BuildAssetBundles
{
    [MenuItem("Assets/Build AssetBundles")]
    static void BuildAllAssetBundles()
    {
        string assetBundleDirectory = "Assets/AssetBundles";

        if (!Directory.Exists(assetBundleDirectory))
        {
            Directory.CreateDirectory(assetBundleDirectory);
        }

        BuildPipeline.BuildAssetBundles(
            assetBundleDirectory,
            BuildAssetBundleOptions.None,
            BuildTarget.StandaloneWindows64
        );

        UnityEngine.Debug.Log("AssetBundle构建完成!");
    }
}
```

### 步骤3: 构建

在Unity菜单选择: **Assets → Build AssetBundles**

生成的文件在: `Assets/AssetBundles/moonlight_sword`

---

## 🎮 测试Prefab

### 场景测试

1. 拖拽 `MoonlightSword.prefab` 到场景
2. 调整位置到摄像机视野内
3. 播放场景查看特效
4. 检查碰撞体范围是否合理

### 脚本测试

创建测试脚本 `SwordTester.cs`:

```csharp
using UnityEngine;

public class SwordTester : MonoBehaviour
{
    public GameObject swordPrefab;
    public GameObject auraPrefab;

    void Update()
    {
        // 按空格键生成剑气
        if (Input.GetKeyDown(KeyCode.Space))
        {
            Vector3 pos = transform.position + transform.forward * 2f;
            Instantiate(auraPrefab, pos, transform.rotation);
        }
    }
}
```

---

## ⚠️ 常见问题

### Q: 刀模型显示全黑?
A: 检查场景光照设置,添加Directional Light。或者检查材质的Shader是否正确。

### Q: 发光效果看不见?
A:
1. 确保使用Standard Shader
2. 检查_EMISSION关键字是否启用
3. 增加EmissionColor的强度值
4. 在Window → Rendering → Lighting Settings中启用Auto Generate

### Q: 粒子不显示?
A:
1. 检查粒子系统是否在播放
2. 确认Max Particles不为0
3. 检查渲染顺序(Render Queue)
4. 确保材质正确设置

### Q: 碰撞体范围不准确?
A:
1. 在Scene视图开启Gizmos查看碰撞体
2. 手动调整BoxCollider的Size和Center
3. 使用OnDrawGizmos可视化调试

### Q: AssetBundle加载失败?
A:
1. 检查平台是否匹配(Windows/Mac/Linux)
2. 确认AssetBundle名称正确
3. 使用AssetBundle.GetAllLoadedAssetBundles()检查加载状态

---

## 📚 参考资源

### Unity官方文档
- [Prefab系统](https://docs.unity3d.com/Manual/Prefabs.html)
- [粒子系统](https://docs.unity3d.com/Manual/ParticleSystems.html)
- [AssetBundle](https://docs.unity3d.com/Manual/AssetBundlesIntro.html)
- [材质和Shader](https://docs.unity3d.com/Manual/Materials.html)

### 推荐工具
- **ProBuilder**: 建模工具,可以在Unity内创建更复杂模型
- **Amplify Shader Editor**: 可视化Shader编辑器
- **Particle Effect Pack**: 粒子特效库

---

## 📝 检查清单

使用此清单确保模型和特效完整:

### 刀模型
- [ ] 刀身、护手、刀柄模型正确生成
- [ ] 材质颜色符合设计(银白、深蓝、深紫)
- [ ] 发光效果正常显示
- [ ] 尺寸正确(总长1.5米)
- [ ] 碰撞体正确配置
- [ ] 挥击轨迹特效工作正常
- [ ] Prefab已保存

### 剑气特效
- [ ] 月牙形状显示正确
- [ ] 发光颜色为冰蓝色
- [ ] 粒子系统正常播放
- [ ] 光晕效果可见
- [ ] 特效尺寸符合设计(宽2米,高1米)
- [ ] Prefab已保存

### AssetBundle
- [ ] 资源已标记AssetBundle名
- [ ] AssetBundle构建成功
- [ ] 文件大小合理(<10MB)
- [ ] 可以正常加载

---

## 🎯 下一步

模型和特效创建完成后:

1. ✅ 将AssetBundle复制到Mod项目的Assets目录
2. ✅ 在代码中引用这些Prefab
3. ✅ 测试在游戏中的显示效果
4. ✅ 根据实际效果调整参数
5. ✅ 完成最终打包

完成这些步骤后,可以继续 `weapon_msk.md` 中的 **第四阶段:代码实现**。

---

**创建日期**: 2025-12-22
**版本**: 1.0
**状态**: 待执行 ⏳
